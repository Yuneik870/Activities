// TODO: using the function expressions below, refactor them into function declarations
function greet() {
    console.log("Hello!");
  };
  
  greet()//This declares the function's value to be "Hello!" hence it prints the value

  function threeModTwo() {
    console.log(3 % 2);
  };
  
  threeModTwo()//This

  let age = 18;
  function checkDrivingAge() {
    if (age >= 16) {
      console.log(true);
    } else {
      console.log(false);
    }
  };
  
  checkDrivingAge()//This prints out the value declared by the results of the if Else statement 
  //within the function once called.